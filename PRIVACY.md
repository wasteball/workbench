# 隐私说明

更新日期：2026-08-03

Workbench 是本地优先的开源浏览器扩展。项目本身不运营账户、遥测、广告或云端后台，也不会把用户数据发送给项目维护者。

## 保存在当前浏览器中的数据

| 数据 | 保存位置 | 何时写入 |
| --- | --- | --- |
| 主题、菜单、输出偏好和存储配置 | 扩展本地存储 | 用户修改设置时 |
| 新建文档和编辑后的恢复草稿 | IndexedDB | 新建后编辑，或本地/远程文档第一次修改后 |
| 本地文件原始基线 | IndexedDB | 本地文件第一次修改后，用于恢复和判断改动 |
| 最近文档元数据 | IndexedDB | 新建或打开文档时 |
| 文件库记录和分类 | IndexedDB | 上传或在线分享成功后，或用户整理分类时 |
| 未选择记住的 AccessKey Secret | 会话存储或当前运行内存 | 当前浏览器会话需要连接时 |
| 用户选择记住的 AccessKey Secret、自定义请求头值 | 扩展本地存储 | 用户明确保存对应连接时 |

未修改的本地文件正文不会被复制到 IndexedDB。浏览器支持文件句柄时，Workbench 可能保存句柄以便用户授权后重新打开或写回；具体可用性由浏览器决定。

这些数据不使用浏览器同步存储。偏好与文件库备份会包含分类、上传记录和记录中的分享链接，但会移除 AccessKey ID、AccessKey Secret、API 用户标识和请求头值。分享链接本身可能带有限时访问参数，备份文件应由用户自行妥善保管。

## 何时会联网

Workbench 只在下列用户动作中联网：

- 用户主动从网址读取 Markdown；
- 用户测试或使用自己配置的上传网关；
- 用户测试或使用自己配置的阿里云 OSS；
- 用户使用自己配置的 STS 服务获取临时凭证；
- 用户点击网页快捷方式，在独立标签页打开目标网站。

上传和在线分享会把用户选择的文件或当前 Markdown 导出结果发送到当前存储连接。返回链接、访问权限和保留周期由该服务的配置与政策决定。

## 浏览器权限

- `storage`：保存设置、会话凭据和本地工作记录。
- 可选 `clipboardWrite`：在浏览器要求时复制 HTML 或分享文字。
- 可选网站权限：从某个网址读取 Markdown，或访问用户配置的网关、STS 和 OSS Endpoint。Workbench 在实际动作发生时按站点申请，不在安装时申请所有网站。

导出文档和下载分享文件使用浏览器自身的文件保存能力，不申请额外下载权限。拒绝网站权限只会阻止对应联网动作，本地文档和本地导出仍可使用。

## 内容安全

Markdown 预览经过允许列表清理。脚本、内联事件、危险协议和未经允许的标签/属性会被移除。Mermaid 使用严格安全模式；用户网页工具在独立标签页打开，不能读取 Workbench 的文档或存储配置。

## 查看、导出和删除

- 在“设置 → 权限与隐私”备份或恢复偏好、文件库记录和分类。
- 在同一页面清除草稿、文件库记录、设置和已保存凭据。
- 同一扩展直接更新会保留本地数据；卸载扩展会由 Chrome 清除该扩展的本地数据，卸载前需要由用户主动备份。
- 清除本地记录不会删除已经上传到网关或 OSS 的远端对象。

若需删除远端对象，请使用存储服务本身的管理工具，或在 Workbench 明确显示且当前连接具备远端删除能力时执行。

## 第三方服务

Workbench 不控制用户选择的上传网关、STS 服务、阿里云 OSS 或网页快捷方式。使用这些服务前，请阅读其隐私政策并确认数据地域、保留期限、访问权限和删除方式。

隐私问题可通过 GitHub Issue 提出；涉及可利用的安全问题时请改用[安全策略](SECURITY.md)中的私密渠道。

# Workbench 0.1.7

这是一版改动审阅快速定位修复。选择表格改动时，Workbench 现在会定位到屏幕上实际可见的表格审阅卡片；即使重复点击当前项，也会重新定位。

## 下载与安装

普通用户不需要安装开发工具，也不需要运行任何命令。

当前版本是 Chrome 商店上线前的公开预览版：

1. 下载 `workbench-browser-extension-0.1.7-chrome.zip`。
2. 右键 ZIP 文件并选择“全部解压”。
3. 在 Chrome 地址栏打开 `chrome://extensions`。
4. 开启“开发者模式”。
5. 点击“加载已解压的扩展程序”，选择刚刚解压的文件夹。

这是预览期的临时安装方式。Chrome 商店版上线后，将改为在商店中直接安装和自动更新。

## 本次修复

- 点击改动审阅列表中的表格改动，会滚动到可见的表格审阅卡片，不再定位到已隐藏的原表格。
- 只有一处表格改动时，重复点击当前项也会重新定位。
- 重新打开改动审阅，或切换“查看当前”“查看全部”时，会重新定位当前改动。
- 定位会等待审阅卡片完成绘制，长文档中的上一处、下一处切换更稳定。

## 验证情况

- 新增组件交互回归，连续两次选择同一处表格改动，两次均定位到可见审阅卡片。
- 18 个测试文件共 50 项单元测试全部通过。
- STS 测试、ESLint、TypeScript、发布内容扫描和 Chrome 正式构建均通过。

## 数据与权限

- 本次更新没有新增浏览器权限，也没有改变本地文件、云端分享或用户偏好的处理方式。
- 本地文件默认不会上传；只有主动上传或在线分享时，内容才会发送到用户选择的存储服务。
- 不包含 Workbench 自有账号、广告、遥测、内置存储地址或预置密钥。

## 已知限制

- 当前仍需手动加载预览版，正式面向普通用户的 Chrome 商店版本尚未上线。
- 使用兼容式文件选择时，Chrome 重启后可能需要重新选择原文件以恢复读取权限；浏览器中的草稿和文件记录不会因此删除。
- 本地相对图片解析、提示框完整样式、脚注、图片灯箱和外部文件变化检测尚未全部迁移，逐项状态见[功能迁移矩阵](feature-migration.md)。
- 阿里云 OSS 和单位提供的上传连接属于可选功能，仍需用户提供自己的有效配置。

## 发布附件

| 文件 | 用途 | SHA-256 |
| --- | --- | --- |
| `workbench-browser-extension-0.1.7-chrome.zip` | Chrome 预览版 | `57a26f2bbf9fe316c784855d4e8956550d7929bc241314de44913748c463076b` |
| `SHA256SUMS.txt` | 文件校验清单 | 见附件内容 |

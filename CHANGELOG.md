# 变更日志

本项目采用 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的结构，并计划在首个稳定版本后遵循语义化版本。

## [Unreleased]

## [0.1.14] - 2026-08-04

### Changed

- 阅读文档时的分享改为当前页面内弹窗，可直接选择 HTML、Word 或 Markdown 格式，生成并复制后继续停留在原文档和原阅读位置。
- 重新设计文件上传区、上传分类入口和链接下载面板，使文件与分享页面的主次关系更清楚。

### Fixed

- 分享结果会直接写入文件库；自动复制失败时保留已生成链接和手动复制入口，不再中断当前文档操作。
- 修复引用块被浏览器默认外边距额外推入的问题；正文、标题、引用框和表格现在对齐阅读列，仅列表与真正的嵌套内容保留层级缩进。
- 导出 HTML 与应用内阅读使用相同的引用缩进规则，移动端分享弹窗不再被上一条提示遮挡。

## [0.1.13] - 2026-08-04

### Changed

- “通过链接下载”默认收起，需要时再展开，减少文件库首页的操作负担。

### Fixed

- 文件下载不再被额外的网站权限步骤拦截；支持跨域读取的地址直接保存，不支持时仍交给浏览器打开原地址。
- 文件库中的显示名称即使被修改，下载时仍使用上传文件原来的名称；分享按钮默认继续复制“文件名、换行、链接”，并可在设置中切换格式。

## [0.1.12] - 2026-08-04

### Changed

- 全面升级 Chrome 插件视觉系统，以暖中性色、石墨色主操作和克制的主题强调色统一首页、Markdown 工作区、文件库、设置、工具页与插件弹窗。
- 重新梳理侧栏、工具栏、表单、筛选、弹层和移动端布局的间距、圆角、图标、边框与状态层级，并同步优化亮色和暗色主题。

## [0.1.11] - 2026-08-04

### Added

- 设置页新增偏好与文件库备份/恢复；备份包含分类和上传记录，但继续排除 AccessKey、用户标识和请求头密钥。
- 上传成功的队列项新增打开、下载和复制分享文字操作。

### Changed

- 云端连接简化为“API 方式 / 阿里云 OSS”单选切换，每种方式只保留一处配置。
- 文件分类改为列表行内切换，批量移动使用紧凑下拉框，类型标签和筛选条改为更轻量的样式。
- 分享操作统一复制用户设置的分享文字，默认格式为“文件名、换行、链接”。

### Fixed

- 修复真实文件下载时 Blob 地址释放过早，可能导致上传后的文件无法保存的问题；对象地址禁止跨域读取时改为在新标签打开。
- 修复文件夹上传把相对路径当作远端文件名的问题；相对路径现在只用于本地记录和自动分类。

### Performance

- 并发上传共用一次存储连接初始化，阿里云普通上传不再预先请求文件列表；多文件上传停止逐个自动复制并合并进度绘制。

## [0.1.10] - 2026-08-03

### Added

- 文件与分享支持选择或拖入整个文件夹，递归上传全部文件，并按顶层文件夹自动建立分类。
- 文件库新增分类增删改、文件移动、自动类型归类、分类与类型筛选，以及单文件和批量操作。
- 新增对象存储链接下载，可恢复中文文件名、保留原始扩展名，并识别常见中转链接参数。
- 长文件库新增分批显示、回到顶部和回到底部操作。

### Changed

- 文件与分享统一为一个文件库页面，移除“云端文件”切换入口；重命名、分类、移除和全部清空明确只整理浏览器记录。
- 分类和类型筛选始终保持单行，内容超出时可以横向滚动。
- Chrome 下载改用浏览器原生文件保存，不再申请 `downloads` 权限。

### Fixed

- 修复对象存储链接下载时中文名称丢失，以及 Markdown 被 OSS XML 错误内容替代并保存为错误文件的问题。
- 修复部分 Chrome 拖拽环境无法通过目录条目读取文件时没有回退路径的问题。

### Performance

- 文件库首次只绘制 120 条记录，更多记录按需加载；已用 135 条记录验证页面响应和筛选条滚动。

## [0.1.9] - 2026-08-03

### Changed

- 云端分享配置页明确区分旧版上传网关与个人阿里云 OSS，补充最小填写方式和扩展来源 CORS 提示。

### Fixed

- 恢复旧版上传网关的表单协议兼容性，公开读取开关按网关要求发送 `1/0`，不再错误发送 `true/false`。
- 上传失败时保留 HTTP 状态和服务端返回的错误原因；网络错误会明确提示检查插件来源的 CORS 设置。

## [0.1.8] - 2026-08-03

### Fixed

- 修复表格单元格编辑后整篇预览被重新绘制、页面明显抖动的问题；普通表格修改现在只更新对应表格。
- 修复打开改动审阅后右侧上一处、下一处快速切换消失的问题；点击当前序号也会重新定位当前改动。

## [0.1.7] - 2026-08-03

### Fixed

- 修复改动审阅无法快速定位表格改动的问题；重复选择当前项、切换查看方式或重新打开审阅时，都会滚动到可见的表格审阅卡片。

## [0.1.6] - 2026-08-03

### Changed

- 表格改动审阅改为按表头、行和单元格展示，直接标出当前值与保存前的值，不再默认显示 Markdown 源码。
- 后续版本只维护、验收和发布 Chrome 插件；自动检查和 GitHub Release 不再生成 Edge 或 Safari 产物。
- Chrome 会记住阅读外观与工作区显示偏好；导出的 HTML 和在线 HTML 分享会继承当前阅读宽度、字体、字号、主题与强调色。

## [0.1.5] - 2026-08-03

### Added

- 左侧文件区新增“清空已打开文件”，只移除 Workbench 记录，不删除电脑原文件，并保留有未保存改动的文档。
- 改动审阅新增精确到词的增删标记，明确区分保存前内容与当前内容。

### Changed

- 打开文件夹改为替换先前加载的干净文件，同一路径文件重复打开时复用记录，避免文件列表持续累加。
- 大文件夹列表分批显示并延后不可见项目绘制，文件正文仍只在用户点开时读取。
- Mermaid 使用缓存和屏幕外隔离渲染，缩放、适应画布和全屏图标恢复为原有样式。
- Word 代码块分别使用适合中英文的字体，并关闭中文自动字符间距。

### Fixed

- 修复 Mermaid 源码查看区被再次识别为新图表，导致同一图表重复显示的问题。
- 修复 Mermaid 中文中心节点文字遮挡，以及图表边缘在预览或导出时可能被裁切的问题。
- 修复编辑或导出 Word 时预览反复重绘、闪烁和正在编辑的正文失去焦点的问题。
- 修复 Word 代码块中的中文被拉开，以及改动审阅难以识别具体修改内容的问题。

### Performance

- 使用 320 个 Markdown 文件的真实文件夹验证首次加载、重复打开、文件搜索和清空流程。

## [0.1.4] - 2026-08-02

### Added

- 正文编辑新增结构化内容插入和内容块类型转换，并补齐表格增删行列与列对齐操作。
- 左侧文件区新增名称搜索，打开大量文档时可以快速筛选文件。
- Mermaid 复制图片、导出、保存和替换操作新增明确的成功或失败提示。

### Changed

- 表格列宽根据表头和单元格内容自动分配；序号、编号和 ID 等短列收窄，说明类长内容列获得更多空间。
- 预览、独立 HTML、打印/PDF 和 Word 导出共用表格列宽规则，并在窄版心中保留横向滚动以避免挤压。
- 导出按钮默认使用最近一次选择的格式，其他格式仍可从相邻菜单切换。
- 阅读设置、导出菜单和改动审阅等浮层支持点击外部区域或按 Escape 关闭。

### Fixed

- 修复 HTML 导出缺少 Mermaid 内容，以及 Word 导出未嵌入渲染后图表、表格样式较弱和图片可能被裁剪的问题。
- 修复打印/PDF 中图表、折叠内容和表格分页效果不稳定的问题。
- 修复保存取消后仍继续执行、没有文件句柄时总是下载新副本，以及已有句柄未可靠写回原文件的问题。
- 修复表格编辑后查找无法带入选中文字、替换结果缺少反馈和全部替换未清楚显示数量的问题。
- 修复折叠内容展开后边界不清楚，以及 Reading width 改变时表格无法保持可读的问题。

## [0.1.3] - 2026-08-02

### Added

- Markdown 工作区左侧恢复文件夹树和文档目录两个独立区域，并支持按当前阅读位置定位标题。
- 编辑模式支持直接修改正文、选中文字排版、表格单元格编辑，以及内容块的插入、移动、复制和删除。
- 阅读设置新增正文字体、字号、版心宽度和界面颜色，并在浏览器中持久保存。

### Changed

- 默认编辑体验改为在正文中直接操作，整篇 Markdown 源文件作为高级入口保留。
- Mermaid 的查看源码、查看图片、复制图片、缩放、适应画布和全屏操作统一使用图标；功能名称通过悬停提示显示。
- 改动审阅恢复为正文标记、清单定位和原内容就地展开的交互。

### Fixed

- 修复 `flowchart` 图表无法渲染，以及多个 Mermaid 图表并行渲染时互相干扰的问题。
- 修复查找未自动带入正文选区、替换当前项和全部替换失效的问题。
- 修复编辑段落未结束时直接保存、撤销或切换文件可能产生错误内容的问题。
- 修复字体、宽度和界面颜色连续调整时，异步保存可能互相覆盖的问题。
- 修复文件较少时左侧文件区域被目录挤压的问题。

### Performance

- 打开文件夹时先登记文件元数据，只在用户点开文档时读取正文，并忽略依赖、构建和版本控制目录。
- 单次文件夹导入限制为前 2,000 个 Markdown 文件，避免超大目录阻塞页面。

## [0.1.2] - 2026-08-02

### Added

- Markdown 工作区支持直接拖入文件或文件夹，并递归读取文件夹内的文档。
- 新增覆盖阅读、编辑和源码模式的统一查找替换条，支持大小写匹配、结果导航、单处替换和全部替换。
- 新增改动审阅面板，可查看修改前后内容、逐项定位，并撤回单处或全部未保存改动。
- Mermaid 图表支持图表/源码切换、拖动、缩放、适应画布、全屏以及复制 PNG 图片。

### Changed

- Markdown 默认进入阅读模式，“打开文件”成为主入口；文件夹和网址收进其他打开方式。
- 阅读、编辑和源码改为明确的三段模式切换，窄屏编辑时只显示编辑器。
- 长文档按当前位置显示回到顶部或底部按钮，并为未保存改动提供浮动导航。

### Fixed

- 修复多个 Mermaid 图表连续渲染时出现空白或尺寸错误的问题。
- 修复移动端编辑器被隐藏、工具栏拥挤和查找条横向溢出的问题。
- 修复图表高度仍在变化时“回到底部”可能停在旧位置的问题。

## [0.1.1] - 2026-08-02

### Changed

- 普通用户入口改为下载、安装和直接使用，源码环境与构建命令只保留在贡献指南。
- 云端分享默认关闭且不生成空白连接，文档的本地打开、编辑、预览和导出无需任何配置。
- 设置页改用用户能识别的云端分享用语；自定义服务地址、临时授权和身份参数收进高级区域。
- 首页与文件分享页减少内部实现术语，浏览器快捷菜单不再显示 `Popup` 名称。
- GitHub 发布说明与仓库 README 明确区分商店正式安装和预览期手动加载。

### Fixed

- 修复窄屏设置页在内容较短或从桌面宽度切换后出现的纵向拉伸和左侧裁切。
- 修复设置表单样式依赖“我的工具”页面先加载的问题。
- 发布扫描允许默认没有云端连接，同时继续阻止内置地址、身份信息和密钥。

## [0.1.0] - 2026-08-02

### Added

- Chrome、Edge 和 Safari 浏览器扩展构建入口。
- 工具栏 Popup 与完整工作台页面。
- 统一设置中心、主题、菜单排序/隐藏、Popup 快捷项和网页工具。
- 网页快捷方式编辑，以及 Popup 最多三项的明确固定规则。
- 本地优先的 Markdown 文档、恢复草稿和最近文档。
- CodeMirror 源码编辑、阅读/分屏模式、查找替换、撤销与重做。
- GFM、代码高亮、KaTeX、Mermaid 图表查看工具、安全 HTML 清理和文档大纲。
- 阅读态任务勾选会同步更新 Markdown 源码，并进入恢复草稿流程。
- Markdown、独立 HTML、DOCX 导出和浏览器打印/PDF。
- 文件上传队列、本地分享历史和 Markdown 在线分享。
- 已有上传网关、阿里云 OSS AccessKey 和 STS 连接器。
- 配置脱敏导入/导出与本地数据清除。
- 单元测试、三浏览器构建检查和真实 Edge 扩展自动化流程。

### Changed

- 合并原先按 HTML/Word 区分的 Markdown 工作区，格式改为统一导出选项。
- 网页工具改为独立标签页快捷方式，不再把任意远程网页作为扩展内数据通道。
- 本地分享记录与云端对象分开表达，避免把删除本地记录误解为删除远端文件。
- 移动端 Markdown 工具栏改为稳定两行布局，文档列表默认收起。

### Security

- 删除所有内置存储地址、Bucket、用户标识和凭据默认值。
- 长期 AccessKey Secret 支持仅会话使用，配置导出始终脱敏。
- 本地 Markdown 渲染不依赖运行时 CDN 或公共代理。

### Known Limitations

- 上传网关尚缺生产接口契约和真实环境验收。
- Safari 仍需 macOS/Xcode 转换、签名和实机验证。
- 旧版能力的剩余迁移状态见[功能迁移矩阵](docs/feature-migration.md)。

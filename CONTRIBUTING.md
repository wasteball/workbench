# 贡献指南

感谢你改进 Workbench。这个项目面向包含大量非技术用户的群体，因此“能工作”之外，还要求入口简单、状态清楚、数据去向可理解。

## 开始之前

- 对大功能先开 Issue，说明用户问题、预期行为、兼容范围和数据/权限影响。
- 安全漏洞不要公开提交 Issue，请按[安全策略](SECURITY.md)报告。
- 不要在 Issue、测试、截图或提交中加入 AccessKey、token、内部地址或真实用户文件。
- 新功能应先检查[功能迁移矩阵](docs/feature-migration.md)，避免重复实现或破坏既定迁移方向。

## 本地环境

需要 Node.js 22、23 或 24。

```bash
npm ci
npm run dev
```

提交前运行：

```bash
npm run docs:check
npm run lint
npm run check
npm test
npm run build:chrome
```

项目只维护和发布 Chrome 插件。其他浏览器不属于功能实现、兼容性验收或发布范围。

## 工程边界

- 页面通过应用服务访问文档、设置和存储，不直接散落调用 `chrome.*`。
- 浏览器差异放在 `src/platform/`；存储厂商差异放在 `src/connectors/`。
- Markdown 源文本是事实来源。渲染、预览和导出是派生结果，不能各自维护业务副本。
- 新的 Markdown 语法、代码围栏渲染器和导出格式应作为独立扩展加入，并提供失败时的安全源码回退。
- 配置项只在统一设置中心定义和保存。功能页面最多提供“前往设置”的入口。
- 不加载运行时远程代码、公共 CDN 脚本、在线字体或默认跨域代理。
- 不使用任意来源的 `postMessage('*')` 传递文档、文件或凭据。

## 用户体验要求

- 普通路径优先使用“连接存储”“上传文件”等用户语言；API、Bucket、RAM 等词放在需要它们的高级配置中。
- 未配置或无权限时说明原因和下一步，不使用无解释的禁用按钮。
- 网络失败不得影响本地文档、恢复草稿和本地导出。
- 删除本地记录和删除远端对象必须使用不同文案与确认流程。
- Popup 只放快速动作；复杂表单和编辑流程进入完整工作台。
- 新界面需要验证 1440px 桌面和 390px 窄屏，不得出现文字裁切、控件重叠或内部横向滚动。

## 测试要求

- 行为修复需要覆盖最接近风险点的自动化测试。
- Markdown 变更至少覆盖恶意 HTML、链接协议、GFM、公式和重复标题 ID。
- 设置与存储变更必须覆盖配置校验、敏感字段脱敏和未配置状态。
- 文件持久化变更必须证明未修改的本地正文不会被写入 IndexedDB。
- UI 变更应在真实扩展上下文检查控制台和页面异常，并提供必要截图。

## 依赖与许可

新增依赖前说明必要性、包体积、维护状态和许可证。更新依赖后重新生成第三方许可证清单：

```bash
npm run notices
```

不得引入与项目发布方式不兼容的许可证。

## Pull Request 清单

- 说明用户可见变化和不在本次范围内的内容。
- 列出运行过的检查和浏览器。
- 说明新增权限、网络请求、持久化数据或迁移行为。
- 同步更新 README、隐私说明、迁移矩阵或变更日志中的相关项。
- 确认提交中没有凭据、内部地址、公司标识和生成缓存。

# 安全策略

## 支持范围

在 `1.0.0` 之前，仅最新提交和最新预发布版本接受安全修复。公开发布后，本文件会列出仍受支持的具体版本。

## 私下报告

请使用 GitHub 仓库 Security 页中的“Report a vulnerability”提交私密报告。仓库所有者应在首次公开前启用 Private vulnerability reporting。

报告中请包含：

- 受影响的版本、浏览器和操作系统；
- 最小复现步骤与实际影响；
- 是否涉及本地文档、恢复草稿、存储凭据或远端对象；
- 可公开分享的截图、日志或示例文件。

不要提交真实 AccessKey、token、私有文档、内部网址或可访问的存储链接。必要时请使用已撤销的测试凭据和最小化样例。

请不要在修复可用前公开披露可被利用的细节。维护者会通过 GitHub 私密渠道确认范围、协调修复和发布说明；当前项目不承诺固定响应 SLA。

## 安全边界

Workbench 的核心原则：

- 本地文件不会因为“打开”而自动上传。
- Markdown 和导出 HTML 经过统一清理，脚本、事件属性和危险 URL 协议默认移除。
- 扩展不执行用户添加网页中的代码；网页工具只在独立标签页打开。
- 网站访问权限按用户动作申请，不在安装时获取所有网站权限。
- 配置导出会清除 AccessKey、用户标识和请求头值。
- 长期 AccessKey Secret 不同步；用户未选择“记住”时只进入会话存储或内存。
- STS 临时凭证不写入长期设置，并在过期前刷新。
- 运行时代码和样式随扩展打包，不依赖公共 CDN 或默认代理。

## 用户仍需负责的风险

- 浏览器扩展存储不是操作系统钥匙串。选择保存长期 AccessKey 时，同一设备上的恶意软件或有权限的本地用户可能读取它。
- 用户配置的上传网关、STS 服务、阿里云 RAM Policy、Bucket CORS 和对象公开性不由 Workbench 运营或控制。
- 公开对象和签名链接可能被持有链接的人转发；分享前应确认访问方式和有效期。
- 自定义网页工具由目标网站运营，其隐私和安全策略独立于 Workbench。

生产环境建议使用独立 RAM 用户、受限对象前缀、最小权限和短期 STS 凭证。不要使用阿里云主账号 AccessKey。
